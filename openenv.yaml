id: govai-env
name: Government Welfare Optimization Environment
description: AI environment for allocating government welfare schemes under constraints
entry_point: app:app

observation_space:
  type: object

action_space:
  type: object

reward_range: [-10, 10]

tasks:
  - id: easy
    description: High budget, low fraud
  - id: medium
    description: Balanced constraints
  - id: hard
    description: Low budget, high fraud risk